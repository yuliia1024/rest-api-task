let mongoose;_ece‍.x([["StudentSchema",()=>StudentSchema]]);_ece‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const Schema = mongoose.Schema;

       const StudentSchema = new Schema({
    firstName: {
        type: String,
        required: 'Enter a first name'
    },
    lastName: {
        type: String,
        required: 'Enter a last name'
    },
    group: {
        type: String,
        required: true,
        },
});//export default mongoose.model('Students', Students);
