let mongoose,bcrypt;_f82‍.x([["UserSchema",()=>UserSchema]]);_f82‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_f82‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);


const Schema = mongoose.Schema;

       const UserSchema = new Schema({
    username: {
        type: String,
        required: true
    },
    email: {
        type: String,
        required: true
    },
    hashPassword: {
        type: String,
        required: true
    },
    created_date: {
        type: Date,
        default: Date.now
    }
});

UserSchema.methods.comparePassword = (password, hashPassword) => {
    return bcrypt.compareSync(password, hashPassword);
};
