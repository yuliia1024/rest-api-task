let mongoose,TeacherSchema;_9de‍.x([["addNewTeacher",()=>addNewTeacher],["getTeacher",()=>getTeacher],["getTeacherById",()=>getTeacherById]]);_9de‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_9de‍.w("../models/teachers",[["TeacherSchema",["TeacherSchema"],function(v){TeacherSchema=v}]]);

const Teacher = mongoose.model('Teacher', TeacherSchema);

       const addNewTeacher =(req,res)=>{
    let newTeacher = new Teacher(req.body);
    newTeacher.save((err, teacher) => {
        if (err) {
            res.send(err);
        }
        res.json(teacher);
    });
};
       const getTeacher = (req, res) => {
    Teacher.find({}, (err, teacher) => {
        if (err) {
            res.send(err);
        }
        res.json(teacher);
    });

}
       const getTeacherById = (req, res) => {
    Teacher.findById(req.params.teacherID, (err, teacher) => {
        if (err) {
            res.send(err);
        }
        res.json(teacher);
    });
}
