let mongoose,TeacherSchema;_4e8‍.x([["addNewTeacher",()=>addNewTeacher],["getTeacher",()=>getTeacher],["getTeacherById",()=>getTeacherById]]);_4e8‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_4e8‍.w("../models/teachers",[["TeacherSchema",["TeacherSchema"],function(v){TeacherSchema=v}]]);

const Teacher = mongoose.model('Teacher', TeacherSchema);

       const addNewTeacher =(req,res)=>{
    let newTeacher = new Teacher(req.body);
    newTeacher.save((err, teacher) => {
        if (err) {
            return res.send(404, `we have problem with db ${err}`);;
        }
        res.json(teacher);
    });
};
       const getTeacher = (req, res) => {
    Teacher.find({}, (err, teacher) => {
        if (err) {
            return res.send(404, `we have problem with db ${err}`);;
        }
        res.json(teacher);
    });

}
       const getTeacherById = (req, res) => {
    Teacher.findById(req.params.teacherID, (err, teacher) => {
        if (err) {
            return res.send(404, `we have problem with db ${err}`);;
        }
        res.json(teacher);
    });
}
