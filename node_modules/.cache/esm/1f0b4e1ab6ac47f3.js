let express;_b6a‍.w("express",[["default",["express"],function(v){express=v}]]);_b6a‍.i ('dotenv').config({path:__dirname+'/config/.env'})

const app = express();
const PORT= process.env.PORT;

app.get('/',(req,res)=> {
    res.send(`on port ${PORT}`);

});
app.listen(PORT, ()=>{
    console.log(`start on port ${PORT}`);

});

